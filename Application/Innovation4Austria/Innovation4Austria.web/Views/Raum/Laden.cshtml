@model Innovation4Austria.web.Models.FilterModel
@{
    ViewBag.Title = "Laden";
}
@using (Html.BeginForm())
{
    <div class="container" style="margin-top:8em;">
        <div class="row col-md-12 FontSize" onclick="aktualisiereReise">

            <div class="row">
                <div class="col-md-12 form-group">
                    @Html.LabelFor(model => model.BeginnDatum)
                    <div class='input-group date' id='datetimepicker1'>
                        <input type='text' class="form-control" name="BeginnDatum" value="@Model.BeginnDatum.ToShortDateString()" />
                        <span class="input-group-addon">
                            <span class="glyphicon glyphicon-calendar"></span>
                        </span>
                    </div>
                </div>
            </div>
            <div class="col-md-12 form-group">
                @Html.LabelFor(model => model.EndDatum)
                <div class='input-group date' id='datetimepicker1'>
                    <input type='text' class="form-control" name="Enddatum" value="@Model.EndDatum.ToShortDateString()" />
                    <span class="input-group-addon">
                        <span class="glyphicon glyphicon-calendar"></span>
                    </span>
                </div>
            </div>
        </div>
            <div class="col-md-3">
                <div>@Html.DropDownListFor(x => x.Art, new SelectList(Model.Art, "Id", "Bezeichnung"))</div>
            </div>
            <div class="col-md-3">
                @foreach (var item in Model.Ausstattung)
                {

                }
            </div>
        </div>
    
   
    @*<div class="row" id="aktualisiereReise">
            <div class="col-md-12">
                @Html.Partial("_RaumAnzeigen", Model.AlleRaeume)
            </div>
        </div>*@
}
